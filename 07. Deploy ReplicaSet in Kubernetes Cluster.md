# Deploy ReplicaSet in Kubernetes Cluster

1. Create a `ReplicaSet` using `nginx` image with `latest tag` (ensure to specify as `nginx:latest`) and name it `nginx-replicaset`.
2. Apply labels: `app` as `nginx_app`, type as `front-end`.
3. Name the container `nginx-container`. Ensure the replica count is `4`.

**`nginx-replicaset.yaml`**

```yaml
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-replicaset
  labels:
    app: nginx_app
    type: front-end
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nginx_app
      type: front-end
  template:
    metadata:
      labels:
        app: nginx_app
        type: front-end
    spec:
      containers:
        - name: nginx-container
          image: nginx:latest
```

### Apply the ReplicaSet

```bash
kubectl apply -f nginx-replicaset.yaml
```

### Verify

```bash
kubectl get rs nginx-replicaset
kubectl get pods -l app=nginx_app,type=front-end
```
<img width="691" height="173" alt="image" src="https://github.com/user-attachments/assets/98ff4cff-2a97-448e-bb85-1f7ec250ba4a" />
